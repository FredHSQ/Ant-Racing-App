import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    textInformationContainer: {
        flexDirection: 'row',
        alignItems: 'center',
        marginRight: 15
    },
    textInformation: {
        color: "#fff",
        marginRight: 5,
    }
})